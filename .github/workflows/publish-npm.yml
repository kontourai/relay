name: Publish NPM

on:
  workflow_dispatch:

concurrency:
  group: publish-npm
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    steps:
      - name: Require version tag
        run: |
          if [[ "${GITHUB_REF}" != refs/tags/v* ]]; then
            echo "Publish must be dispatched at a v* tag, received ${GITHUB_REF}" >&2
            exit 1
          fi
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          fetch-depth: 0
      - name: Verify tag and ancestry
        run: |
          PACKAGE_VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          test "v${PACKAGE_VERSION}" = "${GITHUB_REF_NAME}"
          git fetch origin main
          git merge-base --is-ancestor "${GITHUB_SHA}" "FETCH_HEAD"

  verify:
    needs: preflight
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ['22', '24']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - run: npm ci --ignore-scripts --no-fund --no-audit
      - run: npm run verify

  package:
    needs: [preflight, verify]
    runs-on: ubuntu-latest
    outputs:
      filename: ${{ steps.pack.outputs.filename }}
      sha512: ${{ steps.pack.outputs.sha512 }}
      package_name: ${{ steps.pack.outputs.package_name }}
      package_version: ${{ steps.pack.outputs.package_version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
      - run: npm ci --ignore-scripts --no-fund --no-audit
      - run: npm run build
      - name: Build exact package artifact
        id: pack
        run: |
          PACKAGE_DIR="${RUNNER_TEMP}/relay-package"
          mkdir -p "${PACKAGE_DIR}"
          FILENAME=$(npm pack --pack-destination "${PACKAGE_DIR}" --json | node -e 'let d=""; process.stdin.on("data",c=>d+=c).on("end",()=>console.log(JSON.parse(d)[0].filename))')
          SHA512=$(shasum -a 512 "${PACKAGE_DIR}/${FILENAME}" | awk '{print $1}')
          PACKAGE_NAME=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).name")
          PACKAGE_VERSION=$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")
          echo "filename=${FILENAME}" >> "${GITHUB_OUTPUT}"
          echo "sha512=${SHA512}" >> "${GITHUB_OUTPUT}"
          echo "package_name=${PACKAGE_NAME}" >> "${GITHUB_OUTPUT}"
          echo "package_version=${PACKAGE_VERSION}" >> "${GITHUB_OUTPUT}"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: relay-package-${{ github.ref_name }}
          path: ${{ runner.temp }}/relay-package/${{ steps.pack.outputs.filename }}
          if-no-files-found: error
          retention-days: 1
          compression-level: 0

  publish:
    needs: [preflight, verify, package]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check published version
        id: published
        env:
          PACKAGE_NAME: ${{ needs.package.outputs.package_name }}
          PACKAGE_VERSION: ${{ needs.package.outputs.package_version }}
        run: |
          if npm view "${PACKAGE_NAME}@${PACKAGE_VERSION}" version >/dev/null 2>&1; then
            echo "published=true" >> "${GITHUB_OUTPUT}"
            echo "${PACKAGE_NAME}@${PACKAGE_VERSION} is already published; skipping."
          else
            echo "published=false" >> "${GITHUB_OUTPUT}"
          fi
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        if: steps.published.outputs.published != 'true'
        with:
          node-version: '24'
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        if: steps.published.outputs.published != 'true'
        with:
          name: relay-package-${{ github.ref_name }}
          path: ${{ runner.temp }}/relay-package
      - name: Verify artifact digest
        if: steps.published.outputs.published != 'true'
        env:
          EXPECTED_SHA512: ${{ needs.package.outputs.sha512 }}
          PACKAGE_FILENAME: ${{ needs.package.outputs.filename }}
        run: |
          ACTUAL_SHA512=$(shasum -a 512 "${RUNNER_TEMP}/relay-package/${PACKAGE_FILENAME}" | awk '{print $1}')
          test "${ACTUAL_SHA512}" = "${EXPECTED_SHA512}"
      - name: Publish with provenance
        if: steps.published.outputs.published != 'true'
        env:
          PACKAGE_FILENAME: ${{ needs.package.outputs.filename }}
        run: npm publish "${RUNNER_TEMP}/relay-package/${PACKAGE_FILENAME}" --access public --provenance --ignore-scripts
