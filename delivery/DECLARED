[
  {
    "scope": "author:briananderson1222 branch-prefix:chore/adopt-trust-verify",
    "reason": "The adoption change itself: adds this workflow and this file. Config only; no source change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own Trust Verify runs the fresh verify. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T21:30:00Z"
  },
  {
    "scope": "author:dependabot[bot] branch-prefix:dependabot/",
    "reason": "Dependabot dependency-bump PR; no agent delivery involved, nothing to reconcile. Compound scope binds the platform-set actor identity to the bot's branch namespace per ADR 0022's fork-PR guidance, paired per the 2026-07-03 addendum. Provisioned AT adoption rather than rediscovered by breaking \u2014 flow-agents#1017's three worked examples are why.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T21:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/ts7-canary",
    "reason": "TypeScript 7 (tsgo) canary for the portfolio: the dev-dependencies group bump (typescript 7.0.2, @types/node 26, @ai-sdk/provider 4) plus the one coupled change the bot cannot author \u2014 tsgo no longer auto-includes @types/node, so tsconfig gains an explicit types:[node]. Full npm run verify green under the new toolchain. Supersedes the bot's red #37. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-28T02:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/tv-enforce",
    "reason": "Removes missing-bundle-policy: advisory from the Trust Verify workflow so it inherits the action default (required). Advisory was the adoption stance; with Trust Verify now a required status check in this repo's branch protection, advisory mode is a hole: a live gate probe on a sibling repo (a PR deliberately carrying no bundle and no exemption) passed Trust Verify green and sat MERGEABLE, and this repo runs the identical configuration. One workflow line, no source or behaviour change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the sibling repo's probe re-run under required mode records the negative proof for the shared configuration. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner said fine-to-arm pending working-system evidence; this is the evidence loop)",
    "declared_at": "2026-07-28T19:45:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/flow-agents-local-kit",
    "reason": "Pins @kontourai/flow-agents 5.5.0 as an exact local devDependency and adds the workflow:sidecar / workflow:validate-artifacts scripts, matching the one proven configuration (the marketing-site repo). With bundle-required now enforced portfolio-wide, this repo demanded delivery bundles while providing no declared means to author them: bundle authoring worked only on machines with a coincidental global kit install, and sandboxed delegates without network access could not produce bundles at all. Dev-tooling-only change: no source, no behaviour, no shipped-dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the kit's bins resolve from the local install. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted rollout recommendation)",
    "declared_at": "2026-07-28T21:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/kit-workflow-triggers",
    "reason": "Kit routing declaration (builder.build workflow triggers): adds kits/ kit.json + catalog.json only, byte-identical to the merged veritas#208 reference implementation and to the sibling repo's twin PR. New config files only; no source or behaviour change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own verify (22/24) matrix is green, and byte-identity with the merged veritas#208 payload carries its reviewed evidence. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (owner direction, 2026-08-05 session: merge PRs red only on the Trust Verify gate)",
    "declared_at": "2026-08-05T20:35:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/pnpm-migration",
    "reason": "Wave 2 of the owner-directed portfolio pnpm 11 migration (kontourai/station#516). Installer-only change: package-lock.json converted with pnpm import to pnpm-lock.yaml with zero drift (211 npm-resolved name@version pairs, 211 identical, 0 divergent, and 0 pnpm-only entries in the reverse direction); packageManager pinned to pnpm@11.25.0; dependency install scripts blocked by default via pnpm-workspace.yaml allowBuilds, whose single entry esbuild@0.28.1 is justified by a real ERR_PNPM_IGNORED_BUILDS refusal naming exactly it (transitive through @kontourai/flow-agents 5.5.0); minimumReleaseAge 1440 added. autoInstallPeers is left at pnpm's default because npm had already installed the optional peer @anthropic-ai/sdk 0.74.0 into package-lock.json, so the default resolves the same graph. CI, Trust Verify and the publish verify/package jobs install with pnpm install --frozen-lockfile behind a SHA-pinned pnpm/action-setup; publishing itself stays on npm publish --provenance from the same prebuilt tarball, unchanged. No source change and no shipped-dependency change, so there is nothing for a bundle to reconcile. Verification in lieu of a bundle: npm run verify is green in the pnpm-installed tree (typecheck clean, node --test 39 pass / 0 fail across 5 suites, npm pack --dry-run clean); the packed file list is the same 48 files as the published @kontourai/relay 0.6.0 tarball with no pnpm-* file and no lockfile added; and two fault injections confirm the new gate has teeth, an unmatched devDependency specifier failing with ERR_PNPM_OUTDATED_LOCKFILE and removal of the allowBuilds entry failing with ERR_PNPM_IGNORED_BUILDS naming esbuild@0.28.1. This PR's own CI runs the full verify matrix on both node 22 and 24. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-03 session: owner-directed pnpm migration across all repos, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T01:31:11Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:ci/publish-path-dry-run",
    "reason": "Adds a publish-path dry-run job to ci.yml, rolled out from kontourai/ui#70. After the org-wide npm -> pnpm 11 move, every migrated repository had proven install, build, test and pack, and none had proven publish: npm publish runs only on a tag, against a pnpm-installed tree, under a setup-node that also sets registry-url -- a combination the verify jobs never exercise, and it is that .npmrc which npm publish reads. This job mirrors that environment on every pull request and stops one step short of the registry write. It cannot publish: --dry-run performs no write, the job requests no id-token permission so provenance cannot be minted, and no registry credential is provided. Workflow-only change: no source, no dependency, no lockfile and no shipped content is touched, so nothing for a bundle to reconcile. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: the job is proven green on a hosted runner in ui#70, where its output rather than its exit code is the evidence -- 'npm notice total files: 289' then 'Publish path proven from a pnpm-installed tree: 289 files assembled', with the registry declining only the already-published version. Fault injections recorded there: a type error making prepack fail is reported as 'assembled no tarball'; crafted logs prove the strict branch rejects a 403 while tolerating only the named version-exists refusal, and rejects a zero-file package that exits zero. ui#70's first CI run FAILED and that is disclosed rather than hidden: GitHub runs run: blocks as bash -e, which set -uo pipefail does not clear, so npm's expected non-zero exit killed the step and every assertion below was unreachable -- the gate had no body on CI while passing locally, because it had been tested with bash rather than bash -e. The version rolled out here carries the || STATUS=$? fix and the comment explaining why. The job's action pins follow THIS repository's existing convention rather than importing ui's. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-04 session: owner-directed publish-path gate rollout, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T05:19:11Z"
  }
]
